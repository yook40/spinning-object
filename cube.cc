#include <iostream>
#include <vector>
#include <unistd.h>
using namespace std;

class Cube {
	...
	public:
		Cube() {}
		void spin();
};

int main(void) {
	// horizontal rotation matrix:
	// 
	vector<vector<char>> cube(50, vector<char>(50, '@'));
}
